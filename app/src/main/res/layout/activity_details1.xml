<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <variable
            name="details1Sample"
            type="com.lgvalle.material_animations.Sample" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_horizontal"
        android:orientation="vertical">


        <android.support.v7.widget.Toolbar
            android:id="@+id/toolbar"
            style="@style/MaterialAnimations.TextAppearance.Title"
            android:layout_width="match_parent"
            android:layout_height="?android:attr/actionBarSize"
            android:background="?android:colorPrimary"
            android:elevation="@dimen/elevation_header">

            <TextView
                android:id="@+id/title"
                style="@style/MaterialAnimations.TextAppearance.Title.Inverse"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical|start"
                android:text="@{details1Sample.name}" />

        </android.support.v7.widget.Toolbar>

        <ImageView
            android:id="@+id/square_red"
            style="@style/MaterialAnimations.Icon.Big"
            android:src="@drawable/circle_24dp"
            app:colorTint="@{details1Sample.color}" />

        <TextView
            android:id="@+id/sample1_title"
            style="@style/Base.TextAppearance.AppCompat.Large"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Sample 1" />

        <TextView
            style="@style/MaterialAnimations.Text.Body"
            android:text="Sample Activity demonstrating how to add enter and exit transitions programmatically" />

        <Button
            android:id="@+id/sample1_button1"
            android:text="Exit (setting return transition)"
            style="@style/MaterialAnimations.Button" />

        <Button
            android:id="@+id/sample1_button2"
            style="@style/MaterialAnimations.Button"
            android:text="Exit (without setting return transition)" />


    </LinearLayout>
</layout>